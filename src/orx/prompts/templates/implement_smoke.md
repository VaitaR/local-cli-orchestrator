# Smoke Implement Prompt

This is a speed-focused smoke run. Do the minimum possible work.

## Task
{{ task }}

## Required Action
1. Edit exactly one file: `src/__init__.py`.
2. Append one line: `# smoke: pipeline quick check`.
3. If that exact line already exists, append `# smoke: pipeline quick check 2` instead.
4. Do not modify any other files.
5. Do not run tests, linters, or formatters.

## Output Rules
- Apply the change directly to the filesystem.
- Keep the patch very small.
